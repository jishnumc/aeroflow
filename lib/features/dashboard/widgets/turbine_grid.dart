import 'package:flutter/material.dart';

class TurbineGrid extends StatelessWidget {
  const TurbineGrid({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
