import 'package:flutter/material.dart';

class TurbineCard extends StatelessWidget {
  const TurbineCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
